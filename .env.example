# =============================================================================
# F3 SuiteScript Dev Assist - Environment Configuration
# =============================================================================
# Copy this file to .env and fill in your values:
#   cp .env.example .env
# =============================================================================

# -----------------------------------------------------------------------------
# NetSuite Account Configuration
# -----------------------------------------------------------------------------
# Your NetSuite Account ID
# Format: XXXXXXX for production, XXXXXXX_SBX for sandbox (e.g., 4073908_SB4)
NETSUITE_ACCOUNT_ID=YOUR_ACCOUNT_ID

# NetSuite DevAssist API URL
# Replace YOUR_ACCOUNT_ID with your actual account ID
NETSUITE_API_URL=https://YOUR_ACCOUNT_ID.app.netsuite.com:443/api/internal/devassist/chat/completions

# -----------------------------------------------------------------------------
# OAuth 2.0 Configuration
# -----------------------------------------------------------------------------
# Consumer Key / Client ID from your NetSuite Integration Record
# Get this from: Setup > Integration > Manage Integrations > Your Integration
NETSUITE_CLIENT_ID=YOUR_CONSUMER_KEY

# Redirect URI (must match exactly what's in your Integration Record)
NETSUITE_REDIRECT_URI=http://localhost:3000/auth/callback

# OAuth Scopes (comma-separated, no spaces)
NETSUITE_SCOPE=restlets,rest_webservices

# OAuth Discovery URL (for fetching authorization/token endpoints)
# Replace YOUR_ACCOUNT_ID with your actual account ID
NETSUITE_DISCOVERY_URL=https://YOUR_ACCOUNT_ID.suitetalk.api.netsuite.com/.well-known/oauth-authorization-server

# -----------------------------------------------------------------------------
# Server Configuration
# -----------------------------------------------------------------------------
# Port for the local server (default: 3000)
PORT=3000
